<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
        <title>Administrador</title>
        <link rel="stylesheet" href="client/css/mostrar.css"/>
    </head>
    <body>

        <nav>
            <div class="perfil">

                <div>
                    <a href="">
                        <img src="imagenes/icono-usuario.png" alt="">
                    </a>
                </div>

                <div>
                    <a href="#"></a>
                </div>
            </div>

            <div>
                <a href="Cerrar" class="cerrar">Cerrar sesión</a>
            </div>
        </nav>

        <div class="contenedor">
            <h1>Usuarios</h1>
            <table class="tabla-usuarios">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Nombre</th>
                        <th>Email</th>
                        <th>Contraseña</th>
                        <th>Editar / Borrar</th>
                        <th>Ver mensajes</th>
                        <th>Dar de baja</th>
                    </tr>
                </thead>
                <tbody>
                    <% data.users.map(user => { %>
                    <tr>
                        
                        <td><%= user.id %></td>
                        <td><%= user.nombre %></td>
                        <td><%= user.email %></td>
                        <td><%= user.passw %></td>
                        <td> <a href="/updateUserForm/<%= user.id %>" class="editar">Editar</a> / 
                             <a href="/deleteUser/<%= user.id%>" class="borrar">Borrar</a> </td>
                        <td><a href="" class="mensaje">Mensajes</a></td>
                        <td><a href="/unsubscribe/<%= user.id %>?status=false">Dar de baja</a></td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
            <br>
            <div>
                <a href="agregarUsuarios.jsp" class="agregar">Agregar usuarios</a>
            </div>
            <br><br><br>

            <h1>Administradores</h1>
            <table class="tabla-administradores">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Nombre</th>
                        <th>Email</th>
                        <th>Contraseña</th>
                    </tr>
                </thead>
                <tbody>
                    <% data.admins.map(admin => { %>
                    <tr>                        
                        <td><%= admin.id %></td>
                        <td><%= admin.nombre %></td>
                        <td><%= admin.email %></td>
                        <td><%= admin.passw %></td>

                    </tr>
                    <% }) %>
                </tbody>
            </table>
            <br>
            <div>
                <a href="agregarAdministradores.jsp" class="agregar">Agregar administradores</a>
            </div>
            <br>
            <br>

            <h1>Usuarios dados de baja</h1>
            <table class="tabla-usuarios">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Nombre</th>
                        <th>Email</th>
                        <th>Contraseña</th>
                        <th>Editar / Borrar</th>
                        <th>Ver mensajes</th>
                        <th>Dar de alta</th>
                    </tr>
                </thead>
                <tbody>
                    <% data.unsubscribeUsers.map(user => { %>
                    <tr>
                        
                        <td><%= user.id %></td>
                        <td><%= user.nombre %></td>
                        <td><%= user.email %></td>
                        <td><%= user.passw %></td>
                        <td> <a href="/updateUserForm/<%= user.id %>" class="editar">Editar</a> / 
                             <a href="/deleteUser/<%= user.id%>" class="borrar">Borrar</a> </td>
                        <td><a href="" class="mensaje">Mensajes</a></td>
                        <td><a href="/subscribe/<%= user.id %>?status=true">Dar de alta</a></td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
            <br>
            <div>
                <a href="agregarUsuarios.jsp" class="agregar">Agregar usuarios</a>
            </div>
            <br><br><br>

            
        </div>  


    </body>
</html>
